title: Lab synthetic ContainerDeployFromUntrustedImage on tagged canary (honey_container) — T1610
id: lab-container-deploy-001
status: experimental
description: >
  Lab-synthetic Sigma rule. Fires when an audit-channel event for the canary
  lure (honey_container) is observed with the metatron_run_id tag and the
  expected event_type matches ContainerDeployFromUntrustedImage. Used to validate
  detection coverage for techniques that cannot be exercised by Linux atomics.
references:
  - https://attack.mitre.org/techniques/T1610
author: metatron-lab-harvester
date: 2026/04/27
tags:
  - attack.t1610
  - lab.canary
  - lab.container_runtime_audit
logsource:
  product: lab_audit
  service: container_runtime_audit
  category: container_deploy
detection:
  selection:
    event_type: 'ContainerDeployFromUntrustedImage'
    tags.metatron_run_id|exists: true
  condition: selection
falsepositives:
  - None expected when canary is tagged metatron and safe_to_delete=true
level: high
