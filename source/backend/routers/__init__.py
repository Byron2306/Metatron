# Anti-AI Defense System - API Routers
# Package init is intentionally lightweight so individual routers can be
# imported in isolation (e.g. during adversarial-harness test runs) without
# pulling in every production dependency.  Import routers explicitly in
# main.py or wherever the full FastAPI application is assembled.
