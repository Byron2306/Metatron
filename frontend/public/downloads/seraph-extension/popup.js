chrome.storage.local.get(['blockedCount','alertsCount'],(data)=>{document.getElementById('blocked').textContent=data.blockedCount||0;document.getElementById('alerts').textContent=data.alertsCount||0;});
